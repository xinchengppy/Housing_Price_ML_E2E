name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/65985976686/locations/global/workloadIdentityPools/github-pool/providers/github-actions'
          service_account: 'housing-price-ml-service@housing-price-ml-e2e.iam.gserviceaccount.com'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      # Build and push housing-api image
      - name: Build and Push housing-api
        run: |
          IMAGE_TAG=${GITHUB_SHA}
          AR_HOST=us-central1-docker.pkg.dev
          PROJECT_ID=housing-price-ml-e2e
          REPO_NAME=housing-price-ml-e2e
          IMAGE_NAME=housing-api

          docker build -t $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:$IMAGE_TAG -f Dockerfile .
          docker push $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:$IMAGE_TAG

          docker tag $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:$IMAGE_TAG $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:latest
          docker push $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:latest

      # Build and push housing-streamlit image
      - name: Build and Push housing-streamlit
        run: |
          IMAGE_TAG=${GITHUB_SHA}
          AR_HOST=us-central1-docker.pkg.dev
          PROJECT_ID=housing-price-ml-e2e
          REPO_NAME=housing-price-ml-e2e
          IMAGE_NAME=housing-streamlit

          docker build -t $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:$IMAGE_TAG -f Dockerfile.streamlit .
          docker push $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:$IMAGE_TAG

          docker tag $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:$IMAGE_TAG $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:latest
          docker push $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:latest

      # Deploy to Cloud Run - API
      - name: Deploy housing-api to Cloud Run
        run: |
          IMAGE_TAG=${GITHUB_SHA}
          AR_HOST=us-central1-docker.pkg.dev
          PROJECT_ID=housing-price-ml-e2e
          REPO_NAME=housing-price-ml-e2e
          IMAGE_NAME=housing-api

          gcloud run deploy housing-api \
            --image $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:$IMAGE_TAG \
            --platform managed \
            --region us-central1 \
            --service-account housing-price-ml-service@housing-price-ml-e2e.iam.gserviceaccount.com \
            --set-env-vars GCS_BUCKET=housing-price-ml-e2e-xcluo \
            --allow-unauthenticated

      - name: Get API URL
        id: api-url
        run: |
          API_URL=$(gcloud run services describe housing-api --region=us-central1 --project=housing-price-ml-e2e --format="value(status.url)")
          echo "api_url=$API_URL" >> $GITHUB_OUTPUT

      # Deploy to Cloud Run - Streamlit
      - name: Deploy housing-streamlit to Cloud Run
        run: |
          IMAGE_TAG=${GITHUB_SHA}
          AR_HOST=us-central1-docker.pkg.dev
          PROJECT_ID=housing-price-ml-e2e
          REPO_NAME=housing-price-ml-e2e
          IMAGE_NAME=housing-streamlit

          gcloud run deploy housing-streamlit \
            --image $AR_HOST/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:$IMAGE_TAG \
            --platform managed \
            --region us-central1 \
            --service-account housing-price-ml-service@housing-price-ml-e2e.iam.gserviceaccount.com \
            --set-env-vars API_URL=${{ steps.api-url.outputs.api_url }}/predict,GCS_BUCKET=housing-price-ml-e2e-xcluo \
            --allow-unauthenticated